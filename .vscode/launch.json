{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/assign", // 실행 파일 경로
            "args": [],
            "cwd": "${workspaceFolder}", // 현재 작업 디렉토리
            "console": "integratedTerminal" // VSCode 통합 터미널 사용
        }
    ]
}
